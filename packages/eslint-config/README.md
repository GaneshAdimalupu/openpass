# @openpass/eslint-config

Shared ESLint configurations used across the OpenPass monorepo.

## Usage

This package provides strict linting rules tailored for Next.js and TypeScript projects within the ecosystem. It ensures consistent code style and prevents common errors.
